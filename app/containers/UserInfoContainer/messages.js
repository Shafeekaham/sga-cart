/*
 * UserInfoContainer Messages
 *
 * This contains all the text for the UserInfoContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserInfoContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserInfoContainer container!',
  },
});
